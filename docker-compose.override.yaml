services:
  # Expose Go API to host for local frontend development (npm run dev)
  forensics:
    ports:
      - "8765:8765"

  # Wiremind UI: Update context for local environment layout
  wiremind-ui:
    build:
      context: ../../WebstormProjects/wiremind-ui
